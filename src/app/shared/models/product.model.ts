export interface ProductModel {
  productId: number;
  productName: string;
  productPrice: string;
  productDescription: string;
  productCategorie: string;
  productUrl: string;
  isActive: boolean
}
